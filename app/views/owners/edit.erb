<h1>Update Owner</h1>

<form action="/owners/<%= @owner.id %>" method="POST">
  <label>Name:</label> <input type="text" name="owner[name]" id="owner_name" value="<%= @owner.name %>">

  <p>choose an existing pet:</p>

  <% @pets.each do |pet| %>
    <input type="checkbox" name="owner[pet_ids][]" id="<%= pet.id %>" value="<%= pet.id %>" <%='checked' if @owner.pets.include?(pet) %>>
      <%= pet.name %>
    </input>
  <% end %>

  <p>and/or, create a new pet:</p>

  <label>Pet Name:</label> <input type="text" name="pet[name]" id="pet_name">

  <br><br>

  <input type="submit" id="submit" value="Update Owner">
</form>
