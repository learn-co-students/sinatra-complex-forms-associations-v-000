<h1>Update existing Owner</h1>

<form action="/owners/<%= @owner.id %>" method="POST">
  <label>Name:</label>

  <br></br>

  <input type="text" name="owner[name]" value="<%= @owner.name %>">

  <br></br>
  <strong>Choose an existing pet:</strong>
  <p>
    <% Pet.all.each do |pet| %>
      <input type="checkbox" name="owner[pet_ids][]" id="<%= pet.id %>"  value="<%= pet.id %>" <%= "checked" if @owner.pets.include?(pet) %>><%= pet.name %>
    <% end %>
  </p>
  <strong>and/or create a new pet:</strong>
  <p><input type="text" name="pet[name]"></p>

  <input type="submit" value="Update Owner">
</form>