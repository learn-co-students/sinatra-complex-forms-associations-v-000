<h1>Update Owner</h1>

<form action="/owners/<%=@owner.id%>" method="POST">
  <label>Name:</label>
  <br></br>
  <input type="text" name="owner[name]" id="owner[name]" value="<%=@owner.name%>">
  <br></br>
  <label>Chose an existing pet:</label>
  <br>
  <% Pet.all.each do |pet| %>
    <input type="checkbox" name="owner[pet_ids][]" value="<%=pet.id%>" id="<%=pet.id%>" <%='checked' if @owner.pets.include?(pet)%>><%=pet.name%></input>
    <br>
  <% end %>
  <br>
  <h3><label>And/or create a new pet:</label></h3>
  <label>Name:</label>
  <input type="text" name="pet[name]">
  <br></br>
  <input type="submit" value="Update Owner">
</form>
