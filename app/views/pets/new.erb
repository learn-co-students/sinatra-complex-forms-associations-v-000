<h1>Create a new Pet</h1>

<form action="/pets" method="POST">
  <label>Name:</label>

  <br></br>

  <input id="pet_name" type="text" name="pet[name]">

  <br></br>

  <label>Choose an existing owner:</label>

  <br></br>

  <!-- Rem - b/c a pet can have only own owner (according to migrations), this is not the same as the
  analogous owners new.erb; eg, there is no array for owner_ids -->
  <%@owners.each do |owner|%>
      <input type="checkbox" name="pet[owner_id]" value="<%=owner.id%>" id="<%=owner.id%>"><%=owner.name%></input>
  <%end%>


  <br></br>
  and/or, create a new owner:
    <br></br>
    <label>name:</label>
      <input id="owner_name" type="text" name="owner[name]"></input>
    <br></br>

  <input type="submit" value="Create Pet">
</form>
