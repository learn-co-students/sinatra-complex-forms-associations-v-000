<h1>Create a new Pet</h1>
<form method="post" action="/pets">
  <p>New Pet Name:</p>
  <p><input id="pet_name" type="text" name="pet[name]"></p>
  <p>Select existing owner:</p>
  <%@owners.each {|owner|%>
    <p><input type="checkbox" name="pet[owner_id]" value="<%=owner.id%>" id="<%=owner.id%>"><%=owner.name%></p>
  <%}%>
  <p>Or create a new owner:</p>
  <p>New Owner Name:</p>
  <p><input id="owner_name" type="text" name="owner[name]"></p>
  <input type="submit" value="Create Pet">
</form>
