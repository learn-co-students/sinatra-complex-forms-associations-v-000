<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Create a new Pet</title>
  </head>
  <body>
    <h2>Create A New Pet</h2>

    <form action="/pets" method="post">
      <label>Name:</label>
      <br></br>
      <input type="text" name="pet[name]" id="pet_name">
      <br></br>
      <label>Select Owners:</label>

      <% @owners.each do |o_obj| %>
        <input type="checkbox" name="pet[owner_id]" value="<%= o_obj.id %>" id="<%=o_obj.id%>"><%= o_obj.name%></input>
      <% end %>

      <br></br>

      <label>and/or create a new owner:</label>
      <br></br>

      <input type="text" name="pet[owner_name]" id="owner_name">

      <br></br>

      <input type="submit" value="Create Pet">



    </form>

  </body>
</html>
