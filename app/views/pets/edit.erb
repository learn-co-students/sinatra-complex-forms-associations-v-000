<h1>Update Pet</h1>


<form action="/pets/<%=@pet.id%>" method="post">
 <label>Name:</label>
 <br><br>
 <input type="text" name="pet[name]" id="pet_name" value="<%=@pet.name%>">

  <br><br>

  <label>Choose and existing owner:</label>

  <br><br>

  <% Owner.all.each do |owner| %>
    <input type="checkbox" name="pet[owner_id]" value="<%= owner.id %>" id="<%= owner.id %>" <%= 'checked' if @pet.owner = owner %>>
    <%= owner.name %>
  <% end %>

  <br><br>

  and/or, create a new owner:

  <br><br>
  <label>name:</label>

  <input type="text" name="owner[name]" id="owner_name">

  <br><br>

  <button type="submit" id="Update Pet">Update Pet</button>
</form>
