<h1>Update Pet</h1>
<form action="/pets/<%=@pet.id%>" method="post">
  <input id="hidden" type="hidden" name="_method" value="patch">
  <input type="text" name="pet[name]" id="pet_name" value="<%=@pet.name%>">
  <p>Select an existing owner to choose from:</p>
  <p><%@owners.each do |o|%>
    <label for="pet[owner_id]"><%=o.name%></label><input type="radio" name="pet[owner_id]" id="<%=o.id%>" value="<%=o.id%>" <%="checked" if @pet.owner == o%>>
  <%end%></p>
  <p>Or, create a new owner:</p>
  <p><input type="text" name="owner_name" id="owner[name]"></p>
  <input type="submit" id="submit" value="Update Pet">
</form>

