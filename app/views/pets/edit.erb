<h1>Update Pet</h1>
<form action="/pets/<%=@pet.id%>" method="POST">
  <label for="pet[name]">New Name:</label>
  <input type="text" id="pet_name" name="pet[name]"/>

     <br></br>

  Select an owner:
  <% @all_owners.each do |owner| %>
  <input type="radio" id="<%=owner.id%>" name="pet[owner_id]" value="<%=owner.id%>" <%="checked" if owner.id == @pet.owner_id%>><%=owner.name%> </input>
  <% end %>
  <br></br>

  <label>Or create a new owner:</label>
  <br></br>

  <label>New owner name:</label>
  <input type="text" name="owner[name]" id="owner_name" />
  <br></br>

  <input type="submit" value="Update Pet" />
</form>
