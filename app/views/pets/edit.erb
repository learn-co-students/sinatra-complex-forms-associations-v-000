<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Update A Pet</title>
  </head>
  <body>
    <h2>Update A Pet</h2>

    <form action="/pets/<%= @pet.id %>" method="post">
      <label>Name:</label>
      <br></br>
      <input type="text" name="pet[name]" id="pet_name" value="<%= @pet.name %>">
      <br></br>
      <label>Select Owners:</label>

      <% @owners.each do |o_obj| %>
        <input type="radio" name="pet[owner_id]" value="<%= o_obj.id %>" id="<%=o_obj.id%>" <%= 'checked' if @pet.owner == o_obj %>><%= o_obj.name%></input>
      <% end %>

      <br></br>

      <label>and/or create a new owner:</label>
      <br></br>

      <input type="text" name="pet[owner_name]" id="owner_name">

      <br></br>

      <input type="submit" value="Update Pet">
    </form>
  </body>
</html>
