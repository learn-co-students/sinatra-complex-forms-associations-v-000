<h1>Update Pet</h1>

<form action="/pets/<%= @pet.id %>" method="POST">
<label>Name: </label>
<input type="text" id="pet_name" name="pet[name]" value="<%= @pet.name %>">
<br><br>
Choose an owner:
<br><br>
<% Owner.all.each do |owner| %>
  <input type="checkbox" name="pet[owner_id][]" value="<%= owner.id %>" id="<%= owner.id %>" <%= 'checked' if @pet.owner_id == owner.id %>><%= owner.name %></input>
  <% end %>
  <br><br>
and/or create a new owner: <br><br>
<label>name</label>
<input type ="text" name="owner_name" id="owner[name]">
<br><br>
<input type="submit" value="Update Pet">
</form>s 