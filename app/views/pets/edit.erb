<!-- <h1>Update Pet</h1>

<form action="/pets/<%= @pet.id %>" method="post">

  <input type="text" name="pet[name]" id="pet_name" value="<%= @pet.name %>">

  <br></br>

  <h2>Choose an existing owner: </h2>

  <% Owner.all.each do |owner| %>
    <input type="radio" name="pet[owner_id]" value="<%= owner.id %>" id="<%= owner.name%>" <%= "checked" if @pet.owner== owner %>>
      <%= owner.name %>
  <% end %>

  <br></br>

  <h2>Or, create a new owner:</h2>

  <input type="text" name="owner[name]" value="owner_name">

  <br><br>
  <input type="submit" value="Update Pet">

</form> -->
<h1>Update Pet</h1>

<form action="/pets/<%= @pet.id %>" method="POST">

  <label for="pet_name">Change Name: </label>
  <input type="text" name="pet[name]" id="pet_name" value="<%= @pet.name %>">

  <br></br>

  <label>Choose an existing owner:</label>

  <% @owners.each do |owner| %>
    <input type="radio" name="pet[owner_id]" value="<%= owner.id %>" id="<%= owner.id %>" <%="checked" if @pet.owner == owner %>>
      <%= owner.name %>
    </input>
  <% end %>

  <br></br>

  <label>or, create a new owner:</label>
  <br></br>

  <input type="text" name="owner[name]" id="owner_name">

  <br></br>

  <input type="submit" value="Update Pet">
</form>
